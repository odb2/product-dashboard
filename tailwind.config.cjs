module.exports = {
  content: [
    '*.html',
    '*.js',
    // Add more file paths to include in the purge
  ],
  darkMode: 'media', // or 'class'
  theme: {
    extend: {
      // Add custom theme configurations or extend existing ones
    },
  },
  variants: {
    extend: {
      // Add custom variants or extend existing ones
    },
  },
  plugins: [
    // Add any additional Tailwind CSS plugins here
  ],
};